class Inventory {
    constructor(maxSize) {
        this.maxSize = maxSize;
        this.items = [ new BaseItem("oof", 45) ];

    }

    RemoveItem(itemIndex) {


    }

    AddItem(item) {
        if (this.items.Length == this.maxSize)
            return;

        items[this.item.Length] = item;

    }
}